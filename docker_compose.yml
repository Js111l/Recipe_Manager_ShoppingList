services:
  database:
    container_name: shopping_list_db
    image:
      postgres:latest
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: qwerty
      POSTGRES_DB: shopping_list_db
    ports:
      - "5433:5432"
